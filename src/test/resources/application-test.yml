db-deploy:
  database:
    url: "jdbc:sqlite:${test.db.file:testdb.sqlite}"
    username:
    password:
    driver: org.sqlite.JDBC
    max-pool-size: 10
    connection-timeout: 5000
    idle-timeout: 600000
    max-lifetime: 1800000